## Objective
Create errors.py and the invoices half of the input layer so that load_invoices(path) returns parsed Invoice objects or raises LateFeeError with a one-line message for every malformed invoices file (missing file, wrong header, wrong column count, non-numeric or negative amount, non-ISO due_date).

## Input Files
- ARCHITECTURE.md

## Output Files
- errors.py
- records.py
- tests/__init__.py
- tests/test_records.py

## Satisfies
- none

## Depends On
- none

## Done Check
- python -m unittest tests.test_records -v